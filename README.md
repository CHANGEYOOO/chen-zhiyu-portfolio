# HTML 原型 V0.24 交付

## 已完成

- 固定透明导航与 JOEKUNI Logo；
- 2024《永劫无间》全屏视频首屏；
- 2023《永劫无间》第二个全屏 Statement 画面；
- 两段视频随页面自然滚动，正文独立固定在视口中央并交叉渐隐渐显；
- 两组正文按照滚动进度逐字出现，反向滚动时逐字消退；
- 每段正文逐字显示期间，对应视频固定在视口中；正文显示完成后，继续滚动才推动视频离开；
- 视频画面级放大裁切，去除原片内嵌的上下黑边；
- TVC、About、Contact 三个可访问锚点；
- 桌面、平板和手机的基础响应式结构；
- 视频静音、循环、内联播放、Poster、播放暂停和自动播放失败提示；
- 第二屏视频接近视口后才加载；
- 键盘焦点状态与 `prefers-reduced-motion` 支持。
- 直接访问锚点时正常定位；在 `#work`、`#about` 或 `#contact` 位置刷新时，地址恢复为主页并回到首屏顶部。
- 第二段视频分别使用由桌面版与移动版视频首个稳定画面生成的响应式 WebP Poster，不再共用旧 PNG。
- TVC 已按交付顺序加入 30 个作品项目，采用桌面 3 列、平板 2 列、手机 1 列的响应式网格。
- 每个作品分别生成桌面与移动端 WebP Poster，并使用首个稳定可见画面；内嵌黑边素材已在 Poster 中裁除。
- 30 个作品视频已统一整理为不超过 1080p 的网页上传版，并上传至阿里云 OSS 的 `works/` 目录。
- 播放地址统一使用 `https://media.kjoe.top/works/`；Cloudflare Worker 代理 OSS 响应，保留 Range 分段请求并移除强制下载响应头。
- 作品区默认只显示 Poster；点击封面后才设置视频地址、加载资源并打开单一全屏播放器，关闭时立即暂停并卸载视频。
- 播放器使用原比例完整显示画面，支持系统播放控制、关闭、Esc、加载提示和失败重试；同一时间只存在一个作品视频实例。
- 第二屏正文在视频固定阶段结束、画面开始向上划出时快速淡出，进入 Portfolio 前完全消失。
- 作品卡片的品牌名与类型名使用加粗字重，作品标题继续保持常规字重。
- TVC 标题使用加粗字重；作品区初始固定展示 4 行，桌面、平板、手机分别先显示 12、8、4 个项目，并可通过“查看更多／收起”展开或折叠。
- 顶部导航使用 `Portfolio`，作品区标题继续保留 `TVC`。
- Contact 页面依据简历加入陈智宇的手机与微信同号信息，并提供电话直拨和微信号复制交互。
- TVC 下方新增 `Livestream Set Design` 板块，严格按照 `livestream-projects.json` 渲染 8 个项目与 55 张图片。
- 每个直播项目独占一行；桌面端图片统一高 404px、移动端高 240px，保留原比例并通过 CircularGallery 环形横向画廊滚动。
- 桌面端图库仅在溢出时显示左右箭头，支持悬停连续缓慢滚动、点击移动约 70% 可视宽度；移动端使用手势滑动与图片吸附。
- 修复直接双击 `index.html` 时浏览器禁止读取本地 JSON、导致直播作品载入失败的问题；本地文件模式与网站服务器模式现均可载入同一份 8 项目、55 图片清单。
- 直播空间 8 个项目共 55 张图片已按全量排序规范改用 `项目 id + 组号 + 组内序号` 的标准文件名，并严格按各项目 `images` 数组顺序展示；蜜蜂直播间维持 9 张最终交付图片。
- 将 About 占位区替换为正式的“创作者档案”，加入统一职业定位、个人介绍、四段经历，并将教育信息合并到身份区。
- About 使用暖米色编辑页面、人物与身份双栏、三列方法及编辑式履历网格；移动端按内容阅读顺序切换为单栏。
- 新增陈智宇工作状态 3:4 WebP 肖像，并加入轻量进入动效、无 JavaScript 内容降级及 reduced-motion 支持。
- 移除 About 的 `02` 与 Contact 的 `03` 编号，Contact 原有文案、电话、微信复制和反馈交互保持不变。
- 在 TVC 作品区左上方新增与 About 标签同款的 `PORTFOLIO` 小标题，原有 `TVC` 标题保持不变。
- TVC 标题组与每张作品卡片加入和 About 相同的单次滚动淡入：进入视口时由下方 12px 上移并在 380ms 内显现；“查看更多”按钮保持直接可见。
- Livestream 标题与每个直播项目整行加入同款单次滚动淡入，原有横向滑动、桌面箭头和移动端吸附交互保持不变。
- 所有站点媒体已迁移至 Cloudflare R2：既有作品与图片继续通过不可变的 `media-v0.21` 路径加载，V0.22 更新的首屏与第二屏视频通过 `media-v0.22` 路径加载；Worker 保留视频 Range 分段响应。
- 新增完全独立的 `/admin/dashboard/` 最小只读后台：通过 Cloudflare 账号授权访问，只读取线上前台公开作品 API，显示 30 个 TVC 与 8 个 Livestream；旧后台代码与原有邮箱验证码规则保持不变。
- 直播作品页在保持原生主站结构的前提下，新增独立 React `CircularGallery` 模块；每个项目独占一整行，严格读取 8 个项目与 55 张图片的现有顺序，项目内图片统一高度、宽度按原始比例自适应并保留圆角，不启用自动轮播。

## 浏览方法

在 `prototype` 目录启动任意静态网页服务器后访问 `index.html`。不建议直接双击文件，因为部分浏览器会限制本地文件的视频自动播放或媒体加载。

## 设计还原说明

按照 1440px 桌面稿实现双全屏影像、固定透明导航和左侧正文。两段视频按照正常页面流连续向上滚动；参考 DIVISION 的文字形式，正文独立固定在视口中央，只在两个视频的滚动交接区间切换透明度。设计稿中的放映机占位画面没有作为网页背景，已替换为交付文件指定的两段《永劫无间》素材。

## 响应式处理

移动端没有正式设计稿，当前采用单行紧凑导航、底部身份排版、流体字号和独立视频焦点位置。作品网格在桌面、平板和手机分别使用 3、2、1 列，并在 700px 断点切换到移动端 Poster。该处理属于技术原型假设，需在移动端设计稿确认后复核。

## 视频与性能说明

V0.22 使用重新交付的约 12MB 与 11MB 首屏视频；首屏只预加载 metadata，第二段视频在滚动接近交接区域后才加载。视频统一放大裁切以去除上下黑边，代价是左右画面会被进一步裁去；移动端沿用同一视频并使用独立画面焦点。后续仍建议分别输出桌面与移动端编码及正式 Poster。

## 当前占位内容

个人介绍暂未接入；Contact 页面只使用简历中已确认的联系方式，没有补写未确认的邮箱或其他私人信息。

## 已知问题

- 汇文明朝体和钉钉进步体未随交付文件提供，当前使用系统字体回退；
- 作品 Poster 已从交付视频筛选生成，播放器沿用当前卡片 Poster 直至视频起播；
- 不同浏览器的视频裁切焦点尚需真实画面复核；
- 当前渐隐渐显以整组正文为单位，尚未拆成逐行时间线。
- 放大裁掉黑边后，部分横向画面会被裁去，仍需按最终镜头确定每段视频的焦点位置。

## 待确认事项

- 两款正式字体的网页授权与字体文件；
- 移动端设计稿与视频裁切焦点；
- 首屏与第二屏视频的最终循环时间段；
- 正式 Poster；
- Statement 是否保留中英双语；
- 阿里云 OSS 视频域名的长期缓存、流量与访问策略。

## 下一步

继续在 Safari、Chrome 与移动真机验证全屏播放器、Poster 到起播切换、首屏 GSAP 卡片翻转、直播 React CircularGallery、About Lanyard、GSAP 作品动效和弱网缓冲；当前本地版本为 V0.24 Revision 69，尚未发布。
